extends ../layouts/base.pug

block head
    title Cart 

block body
    if cart.length
        table 
            tr 
                th Product 
                th Price
                th 
            each product of cart 
                tr 
                    td #{product.title}
                    td #{product.price}
                    td Remove
        h2 #{total}
    else
        h1 Your cart is empty 
        a(href="/shop/all-products") Shop Something